name: Android Preview Build (Test)

# Trigger the workflow manually via EAS CLI or automatically on push
on:
  workflow_dispatch:
  push:
    branches: ['staging', 'preview', 'main']

jobs:
  build_android_preview:
    name: Build Android Preview
    type: build
    params:
      platform: android
      profile: preview
